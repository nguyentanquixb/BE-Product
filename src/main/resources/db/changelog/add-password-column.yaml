databaseChangeLog:
  - changeSet:
      id: add-password-column
      author: BE PRODUCT
      changes:
        - addColumn:
            tableName: users
            schemaName: product_liquibase
            columns:
              - column:
                  name: password
                  type: VARCHAR(255)
                  constraints:
                    nullable: false
